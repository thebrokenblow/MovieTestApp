@using Razor.Data
@using Razor.Model
@using Microsoft.EntityFrameworkCore;
@using Microsoft.AspNetCore.Components.Web
@inject Ticket ticket


<div class="row row-cols-4">
    @if (ticket.SelectedSchedule is not null)
    {
        for (int i = 0; i < 10; i++)
        {
            <button @onclick="(e => SelectSchedule(i))"
                   >
                More Info
            </button>
        }
    }
</div>

@code {
    public void SelectSchedule(int i)
    {
        
    }
}